---
import Layout from '../../layouts/Layout.astro';
import AdminDashboardReact from '../../components/AdminDashboard'; 
import { getSupabaseServer } from '../../lib/supabase';

const supabase = getSupabaseServer(Astro.cookies);

// 1. Cek User Login
const { data: { user }, error: authError } = await supabase.auth.getUser();

if (authError || !user) {
    return Astro.redirect('/login');
}

// 2. Cek Role Admin
const { data: profile } = await supabase
    .from('profiles')
    .select('role')
    .eq('id', user.id)
    .single();

if (!profile || profile.role !== 'admin') {
    return Astro.redirect('/'); 
}

// 3. Ambil Data Awal (Categories & Articles saja)
// Promo IG tidak perlu diambil di sini lagi, karena AdminDashboard.jsx mengambilnya sendiri.
const { data: categories } = await supabase.from('categories').select('*').order('name');

const { data: articles } = await supabase
    .from('articles')
    .select('*, profiles(full_name), article_categories(category_id)')
    .order('created_at', { ascending: false }); 
---

<Layout title="Admin Dashboard | MEDIACIVITAS">
    <div class="admin-view-container">
        {/* Hapus prop serverPromos karena sudah tidak dipakai di komponen React */}
        <AdminDashboardReact 
            client:only="react" 
            serverCategories={categories || []} 
            serverArticles={articles || []}
        />
    </div>
</Layout>

<style>
    .admin-view-container {
        width: 100%;
        max-width: 1440px; 
        margin: 0 auto;    
        padding: 40px 20px; 
        min-height: 80vh;  
    }
</style>