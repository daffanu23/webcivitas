---
import { User, Clock } from 'lucide-react';

const { article } = Astro.props;

// --- LOGIC WAKTU (Time Ago) ---
const getRelativeTime = (dateString: string) => {
    const date = new Date(dateString);
    const now = new Date();
    const diffInSeconds = Math.floor((now.getTime() - date.getTime()) / 1000);
    
    const minutes = Math.floor(diffInSeconds / 60);
    const hours = Math.floor(diffInSeconds / 3600);
    const days = Math.floor(diffInSeconds / 86400);

    if (days > 0) return date.toLocaleDateString('id-ID', { day: 'numeric', month: 'short' });
    if (hours > 0) return `${hours} jam yang lalu`;
    if (minutes > 0) return `${minutes} menit yang lalu`;
    return "Baru saja";
};

// Data Dummy (Preview jika article kosong/loading)
const data = article || {
    title: "Judul Berita Placeholder",
    slug: "#",
    cover_url: "https://placehold.co/600x400",
    content: "Lorem ipsum dolor sit amet, consectetur adipiscing elit...",
    created_at: new Date().toISOString(),
    profiles: { full_name: "Penulis" }
};

// Potong teks konten biar ga kepanjangan
const excerpt = data.content 
    ? data.content.replace(/<[^>]*>?/gm, '').substring(0, 80) + "..." 
    : "";
---

<a href={`/berita/${data.slug}`} class="news-card">
    
    <div class="card-image">
        <img src={data.cover_url} alt={data.title} loading="lazy" />
    </div>

    <div class="card-content">
        <h3 class="card-title">{data.title}</h3>
        
        <p class="card-excerpt">{excerpt}</p>
        
        <div class="card-meta">
            <div class="meta-item">
                <User size={14} />
                <span>{data.profiles?.full_name || 'Redaksi'}</span>
            </div>
            <span>-</span>
            <div class="meta-item">
                <span>{getRelativeTime(data.created_at)}</span>
            </div>
        </div>
    </div>

</a>

<style>
    .news-card {
        display: flex;
        flex-direction: column;
        background: #fff;
        text-decoration: none;
        color: inherit;
        
        /* BORDER STYLE SESUAI GAMBAR */
        border: 1px solid #000; /* Border Hitam Tipis */
        border-radius: 12px;    /* Radius agak tumpul */
        overflow: hidden;
        
        transition: transform 0.2s ease, box-shadow 0.2s ease;
        height: 100%; /* Agar tinggi sama rata di grid */
    }

    /* Efek Hover: Naik & Shadow */
    .news-card:hover {
        transform: translateY(-4px);
        box-shadow: 4px 4px 0px #000; /* Shadow Kasar (Brutalist Lite) */
    }

    /* AREA GAMBAR */
    .card-image {
        width: 100%;
        height: 200px; /* Tinggi gambar fix */
        border-bottom: 1px solid #000; /* Garis pemisah gambar & teks */
        overflow: hidden;
    }
    
    .card-image img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        transition: transform 0.5s ease;
    }

    .news-card:hover .card-image img {
        transform: scale(1.05); /* Zoom in dikit pas hover */
    }

    /* AREA TEKS */
    .card-content {
        padding: 20px;
        display: flex;
        flex-direction: column;
        flex-grow: 1; /* Isi sisa ruang vertical */
    }

    .card-title {
        font-size: 1.1rem;
        font-weight: 600;
        line-height: 1.3;
        margin-bottom: 10px;
        color: #111;
        
        /* Batasi max 2 baris */
        display: -webkit-box;
        -webkit-line-clamp: 2;
        -webkit-box-orient: vertical;
        overflow: hidden;
    }

    .card-excerpt {
        font-size: 0.9rem;
        color: #555;
        margin-bottom: 20px;
        line-height: 1.5;
        flex-grow: 1; /* Dorong meta ke paling bawah */
    }

    .card-meta {
        display: flex;
        align-items: center;
        gap: 8px;
        font-size: 0.8rem;
        font-weight: 600;
        color: #666;
        padding-top: 15px;
        border-top: 1px dashed #eee; /* Garis putus-putus tipis di atas meta */
    }

    .meta-item {
        display: flex;
        align-items: center;
        gap: 5px;
    }
</style>