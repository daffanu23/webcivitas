---
import { Facebook, Twitter, Instagram, Github } from 'lucide-react';
const currentYear = new Date().getFullYear();
---

<footer class="site-footer">
    <div class="container footer-content">
        
        <div class="footer-brand">
            <a href="/" class="footer-logo">MEDIA<span style="font-weight: 300;">CIVITAS.</span></a>
            <p class="footer-desc">
                Media independen yang menyajikan berita terkini dengan sudut pandang tajam dan berimbang.
            </p>
        </div>

        <div class="footer-links">
            <h4 class="footer-heading">Jelajahi</h4>
            <a href="/">Beranda</a>
            <a href="/redaksi/tulis">Redaksi</a>
            <a href="/login">Login Admin</a>
        </div>

        <div class="footer-social">
            <h4 class="footer-heading">Ikuti Kami</h4>
            <div class="social-icons">
                <a href="#" aria-label="Twitter"><Twitter size={20} /></a>
                <a href="#" aria-label="Facebook"><Facebook size={20} /></a>
                <a href="#" aria-label="Instagram"><Instagram size={20} /></a>
                <a href="https://github.com" target="_blank" aria-label="GitHub"><Github size={20} /></a>
            </div>
        </div>

    </div>

    <div class="copyright">
        <div class="container">
            &copy; {currentYear} @mediacivitas. All rights reserved.
        </div>
    </div>
</footer>

<style>
    .site-footer {
        border-top: 2px solid var(--fg); /* Garis tegas */
        background-color: var(--bg);
        color: var(--fg);
        padding-top: 60px;
        margin-top: auto; /* Biar selalu di bawah kalau konten dikit */
    }

    .footer-content {
        display: grid;
        grid-template-columns: 2fr 1fr 1fr; /* Layout kolom */
        gap: 40px;
        padding-bottom: 60px;
    }

    /* Responsive: Jadi 1 kolom di HP */
    @media (max-width: 768px) {
        .footer-content { grid-template-columns: 1fr; gap: 30px; text-align: center; }
        .footer-brand { margin: 0 auto; }
        .social-icons { justify-content: center; }
    }

    .footer-logo {
        font-size: 1.5rem;
        font-weight: 900;
        letter-spacing: -1px;
        display: block;
        margin-bottom: 15px;
    }

    .footer-desc {
        font-size: 0.9rem;
        line-height: 1.6;
        color: #555;
        max-width: 300px;
    }

    .footer-heading {
        font-size: 0.85rem;
        text-transform: uppercase;
        letter-spacing: 0.1em;
        margin-bottom: 20px;
        border-bottom: 1px solid #ddd;
        padding-bottom: 10px;
        display: inline-block;
    }

    .footer-links { display: flex; flex-direction: column; gap: 10px; }
    .footer-links a {
        font-size: 0.95rem;
        color: #555;
        transition: color 0.2s;
    }
    .footer-links a:hover { color: var(--fg); text-decoration: underline; }

    .social-icons { display: flex; gap: 15px; }
    .social-icons a {
        color: var(--fg);
        transition: transform 0.2s;
    }
    .social-icons a:hover { transform: translateY(-3px); }

    .copyright {
        border-top: 1px solid #eee;
        padding: 20px 0;
        text-align: center;
        font-size: 0.8rem;
        color: #888;
        background: #fafafa;
    }
</style>